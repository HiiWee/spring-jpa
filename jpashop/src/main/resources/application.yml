spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/develop/db/jpashop
    username: sa
    password:
    driver-class-name: org.h2.Driver


  jpa:
    hibernate:
      ddl-auto: create  # entity? ??? ?? ??
    properties:
      hibernate:
#        show_sql: true # ??? ??? system.out? ?? ??? ????.
#        format_sql: true

#logging:
#  level:
#    org.hibernate.SQL: debug  # hibernate? ???? sql? debug ??? ?
#    org.hibernate.type: trace
